{
  "name": "smart-campus-backend",
  "version": "1.0.0",
  "description": "Smart Campus Portal Backend API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "prod": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "npx prisma generate",
    "vercel-build": "npx prisma generate",
    "postinstall": "npx prisma generate",
    "migrate": "npx prisma migrate deploy && npm run seed",
    "seed": "node src/scripts/seedProductionData.js",
    "render-build": "npm install && npx prisma generate && npx prisma migrate deploy",
    "test": "jest",
    "lint": "eslint src/",
    "format": "prettier --write src/"
  },
  "dependencies": {
    "@prisma/client": "^5.1.1",
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "connect-redis": "^7.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.8.1",
    "express-session": "^1.17.3",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.1",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.4",
    "redis": "^4.6.7",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "eslint": "^8.45.0",
    "jest": "^29.6.1",
    "nodemon": "^3.0.1",
    "prettier": "^3.0.0",
    "prisma": "^5.1.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "prisma": {
    "binaryTargets": [
      "native",
      "linux-musl",
      "debian-openssl-1.1.x"
    ]
  }
}
