version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - cd backend
      - npm install
      - npx prisma generate
run:
  runtime-version: 18
  command: cd backend && npm start
  network:
    port: 8080
    env: PORT
  env:
    - name: NODE_ENV
      value: production